// P2 Dino Eye
// Vari

int ledPin = 10; 
int onDuration = 2500; 
int offDuration = 1500; 

void setup() {
    pinMode(ledPin, OUTPUT); 
}

void loop() {
    digitalWrite(ledPin, HIGH); 
    delay(onDuration); 
    digitalWrite(ledPin, LOW); 
    delay(offDuration); 
}
